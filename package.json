{
    "private": true,
    "type": "module",
    "devDependencies": {
        "uuid": "^13.0.0",
        "z-schema": "^12.2.0"
    },
    "scripts": {
        "clean": "test -f data/gb.json && rm data/gb.json || true && test -f data/all.json && rm data/all.json || true && rm data/*.md || true",
        "format": "npm run clean && node scripts/format.js",
        "markdown": "node scripts/generate-markdown.js",
        "build": "npm run clean && node scripts/aggregate-countries.js && npm run markdown",
        "test:validate": "node scripts/validate.js",
        "test:schema": "npm run clean && npx z-schema 'schema.json' data/*.json",
        "test:schema:ci": "npm run clean && node scripts/generate-schema-ci.js"
    }
}
